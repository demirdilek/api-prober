apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-prober
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/demirdilek/api-prober.git'
    targetRevision: HEAD
    path: 'helm/api-prober'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

