apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard
  labels:
    grafana_dashboard: "1"
data:
  api_prober_dashboard.json: |-
    {{- .Files.Get "dashboards/api_prober_dashboard.json" | nindent 4 }}