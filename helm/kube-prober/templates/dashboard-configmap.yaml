apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-dashboard"
  labels:
    {{ .Values.dashboards.label }}: {{ .Values.dashboards.labelValue | quote }}
data:
  kube_prober_dashboard.json: |-
    {{- .Files.Get "dashboards/kube_prober_dashboard.json" | nindent 4 }}