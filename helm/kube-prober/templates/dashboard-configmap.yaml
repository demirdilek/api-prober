apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-prober-dashboard
  namespace: default
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/name: kube-prober
data:
  kube_prober_dashboard.json: |-
{{ .Files.Get "dashboards/kube_prober_dashboard.json" | indent 4 }}